version: '3.8'

services:
  web:
    container_name: dashboard-kemenag-app
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:80"
    restart: always
    environment:
      - NODE_ENV=production
